# Frontend Environment Variables
# Rename this file to .env.local and fill in your values

# API URL (only needed if not using Vite proxy, e.g., in production)
# VITE_API_URL=https://your-backend-url.com/api

# Sentry Error Monitoring (optional but recommended for production)
# Get your DSN from https://sentry.io - use the same project or create a separate frontend project
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
